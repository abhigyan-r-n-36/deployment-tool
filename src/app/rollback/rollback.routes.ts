import { Routes } from '@angular/router';
import { RollbackComponent } from './rollback.component';

export const rollbackRoutes: Routes = [
  {
    path: '',
    component: RollbackComponent,
  }
];
